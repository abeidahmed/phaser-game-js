export const GRAVITY = 800;
export const MAX_UPWARDS_VELOCITY = -300;
export const UPWARDS_ACCELERATION = -450;
export const TERMINAL_VELOCITY = 400;
export const PIPE_SPACING_Y = 100;
export const TREADMILL_SPEED = -125;

export const CONFIG_WIDTH = document.documentElement.clientWidth;
export const CONFIG_HEIGHT = document.documentElement.clientHeight;
export const GROUND_Y = CONFIG_HEIGHT;
